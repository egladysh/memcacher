# put it in /etc/systemd/system/
# run $systemctl daemon-reload
[Unit]
Description=memcacher service (daemon)
After=network.target

[Service]
ExecStart=~/git/build/memcacher

[Install]
WantedBy=default.target
